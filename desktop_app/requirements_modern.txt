# 🚀 Tracker33 Modern GUI Client - Dependencies
# Современный клиент с системным треем

# Основные зависимости
PyQt6>=6.5.0
aiohttp>=3.8.0
psutil>=5.9.0
requests>=2.25.0

# Отслеживание активности
pynput>=1.7.0
pygetwindow>=0.0.9

# Windows-специфичные зависимости (устанавливаются только на Windows)
pywin32>=306; sys_platform == "win32"

# Опциональные зависимости для уведомлений
plyer>=2.1; sys_platform != "win32" 